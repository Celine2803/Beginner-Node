//package.json is to store metadata for our project
const _=require('lodash');
const example=_.fill([1,2,3,4,5],"celine",1,-1);
console.log(example);

//"lodash": "^4.17.21" is for major,minor,patch
//That being that major is a whole version while minor is for functionality
//while patch is for fixing bugs
//the arrow is for change only in minor an patch
//the tada symbol is for only the patch